# The utility has no compiled artefact of its own; this keeps any
# byte-code cache a future toolchain might drop here out of the index
# even when the tree is consumed outside the repository root.
*.rbc
.byebug_history

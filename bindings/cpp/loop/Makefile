# Delegates to the binding's top-level Makefile so the compiler flags
# and libitb3 wiring stay in one place. `make` here produces ./loop.

all:
	$(MAKE) -C .. loop

clean:
	rm -f loop

.PHONY: all clean
